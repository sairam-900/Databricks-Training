# SQL Practice – Day-1

**Date:** 13/07/2026

## Overview

Completed hands-on SQL practice using an **Employees** dataset. Focused on writing and executing SQL queries to strengthen database querying and data analysis skills.

## Topics Covered

* SELECT statements
* WHERE clause
* GROUP BY
* HAVING clause
* TOP clause
* DISTINCT keyword
* Comparison Operators (`=`, `>`, `<`, `>=`, `<=`, `<>`)
* Logical Operators (`AND`, `OR`, `NOT`)
* IN and NOT IN
* BETWEEN and NOT BETWEEN
* LIKE Operator with Wildcards (`%`)

## Skills Gained

* Retrieved and filtered data from database tables.
* Applied conditional filtering using different SQL operators.
* Performed data aggregation using SQL aggregate functions.
* Grouped records and filtered grouped results using `GROUP BY` and `HAVING`.
* Sorted and retrieved top records using `ORDER BY` and `TOP`.
* Used pattern matching with the `LIKE` operator.
* Improved understanding of SQL query writing and database concepts.

## Dataset Used

**Table:** Employees

The dataset contains employee information, including:

* Employee ID
* Employee Name
* Department
* Salary
* City
* Experience

## Outcome

Successfully practiced fundamental SQL concepts through multiple real-world query exercises, improving confidence in writing SQL statements for data retrieval, filtering, aggregation, and analysis.
